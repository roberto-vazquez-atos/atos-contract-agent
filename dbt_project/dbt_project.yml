name: 'contract_analytics'
version: '1.0.0'
config-version: 2

profile: 'contract_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  contract_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
